[build_sphinx]
source-dir = doc/source
build-dir  = doc/build
all_files  = 1

[upload_sphinx]
upload-dir = doc/build

[tox]
envlist = py37

[testenv]
# TODO: Repeated in setup.py
deps =
    pytest
    scanpy
    pyyaml
commands =
    python setup.py build
    pytest tests
